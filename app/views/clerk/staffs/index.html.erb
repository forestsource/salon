<h1>Staffs#index</h1>
<p>Find me in app/views/staffs/index.html.erb</p>
<% if @staffs.present? %>
<table class="list">
  <tr>
    <th>名前</th>
    <th>自己紹介</th>
    <th>趣味</th>
    <th>得意分野</th>
  </tr>
  <% @staffs.each do |staff| %>
  <tr>
    <td align = "right"><%= staff.member.name %></td>
    <td><%= staff.intro_myself %></td>
    <td><%= staff.interest %></td>
    <td><%= staff.category.category_name %></td>
  </tr>
  <% end %>
</table>
<% else %>
<p>店員情報がありません。</p>
<% end %>
