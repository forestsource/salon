<%@page_title = "メニュー・スタッフ・予約日選択"%>
<h1><%= @page_title %></h1>
<% if @reserved.present? %>
  <table class="list">
  <tr>
    <th>ご予約日</th>
    <th>時間</th>
    <th>メニュー</th>
　　  <th>所要時間</th>
    <th>スタッフ</th>
    <th>お値段</th>
  </tr>
  <% @reserved.each.with_index do |reserve,dx| %>
  <tr>
    <td <td class="td<%=idx%2%> align="right"><%= reserve.day %></td>
    <td class="td<%=idx%2%>"></td><%= reserve.open.time %></td>
    <td class="td<%=idx%2%>"></td><%= reserve.menu.menu_name %></td>
    <td class="td<%=idx%2%>"></td><%= reserve.menu.menu_time %></td>
    <td class="td<%=idx%2%>"></td><%= reserve.staff.member.name %></td>
    <td class="td<%=idx%2%>"></td><%= reserve.menu.price %></td>
  </tr>
  <% end %>
  </table>
<% else %>
現在、予約はありません。
<%end%>

